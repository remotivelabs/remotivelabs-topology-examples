# Terraform state for this server. Give every server a unique prefix so their
# states stay isolated and independently destroyable.
bucket = "your-terraform-state-bucket"
prefix = "topology-single-host-cloud-server-template"
