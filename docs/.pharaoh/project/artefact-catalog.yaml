# Artefact Catalog — declares required/optional fields per type
# Generated by pharaoh-tailor-bootstrap

types:
  feat:
    required_fields: [status]
    optional_fields: [source_doc, tags]
    lifecycle: default

  sysreq:
    required_fields: [status, asil]
    optional_fields: [source_doc, verification_method, tags]
    required_roles: [reviewer, approved_by]
    lifecycle: default

  comp_req:
    required_fields: [status, asil]
    optional_fields: [source_doc, verification_method, satisfies, tags]
    required_roles: [reviewer, approved_by]
    lifecycle: default

  arch:
    required_fields: [status]
    optional_fields: [source_doc, satisfies, tags]
    lifecycle: default

  ecu:
    required_fields: [status, ecu_name]
    optional_fields: [source_doc, tags]
    lifecycle: default

  channel:
    required_fields: [status, channel_name, protocol]
    optional_fields: [signal_database, connects_to, tags]
    lifecycle: default

  model:
    required_fields: [status, source_doc]
    optional_fields: [satisfies, tags]
    lifecycle: default

  tc:
    required_fields: [status, verification_method]
    optional_fields: [source_doc, verifies, tags]
    lifecycle: default

  fmea:
    required_fields: [status, severity, occurrence, detection, rpn]
    optional_fields: [asil, mitigates, tags]
    lifecycle: default
