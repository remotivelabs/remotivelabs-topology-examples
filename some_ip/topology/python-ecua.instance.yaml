schema: remotive-topology-instance:0.7

ecus:
  ecu_a:
    models:
      ecua:
        type: container
        container:
          build:
            dockerfile: ../Dockerfile
          volumes:
            - ../ecus/ecu_a:/app/ecu_a
          working_dir: /app
          command: python -m ecu_a
