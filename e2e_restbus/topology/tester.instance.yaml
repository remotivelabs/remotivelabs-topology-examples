schema: remotive-topology-instance:0.12

containers:
  tester:
    profiles:
      - tester
    build:
      dockerfile: ../Dockerfile
    volumes:
      - ../testsuite:/app
    working_dir: /app
    command: "pytest"
