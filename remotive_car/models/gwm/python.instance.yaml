schema: remotive-topology-instance:0.12

platform:
  includes:
    - ../../platform/remotive-car.platform.yaml

ecus:
  GWM:
    models:
      gwm:
        type: container
        container:
          build:
            args:
              - MODEL_PATH=gwm/python
            target: model
            dockerfile: ../../Dockerfile
          command: python -m gwm
