services:
  ihu:
    privileged: true
    healthcheck:
      test: ["CMD-SHELL", "test -f /dev/shm/cuttlefish-started || exit 1"]
      start_period: 10m
      start_interval: 5s

  tester:
    depends_on:
      ihu:
        condition: service_healthy
